version: 2.0
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.2-jessie-node
    steps:
      - checkout
      - run: echo "Hello World"

deployment:
    master:
        bracn: master
        commands:
            - ./deployment.sh
