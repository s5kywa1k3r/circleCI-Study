version: 2.0
jobs:
    build:
        docker:
            - image: circleci/ruby:2.4.2-jessie-node
        steps:
            - checkout
            - run: echo "Hello World"

    deployment:
        master:
            branch: master
            commands:
                - ./deployment.sh

workflows:
    version: 2
    build-deploy:
        jobs:
            - build:
                filters:
                    branches:
                        only: master
            - deployment:
                requires:
                    - build
                filters:
                    branches:
                        only: master    
